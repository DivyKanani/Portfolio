name: Portfolio Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.X]
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install and Build
        run: |
          npm install
          git config user.name DivyKanani
          git config user.email divykanani9@gmail.com

      - name: Deploy
        run: npm run deploy
